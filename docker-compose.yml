services:
  warden:
    build: .
    image: rtsp-warden:latest
    container_name: rtsp-warden
    restart: unless-stopped
    ports:
      - "8080:8080"     # Web UI
      - "9001:9001"     # MJPEG proxy (optional; only if MJPEG proxy enabled)
    volumes:
      - ./config:/app/config:ro              # config.yaml goes here
      - ./recordings:/app/recordings          # persisted recordings
      - ./data:/app/data                      # SQLite DB lives here
    environment:
      # Use SQLite for simplicity; uncomment to use Postgres
      # WARDEN_DB_URL: postgresql://warden:warden@postgres:5432/warden
      WARDEN_WEB_HOST: 0.0.0.0
      WARDEN_WEB_PORT: 8080
      WARDEN_AUTH_ENABLED: "true"
      TZ: ${TZ:-UTC}
    # depends_on:
    #   postgres:
    #     condition: service_healthy
    # networks:
    #   - warden-net

  # Optional: PostgreSQL for production
  # postgres:
  #   image: postgres:16-alpine
  #   container_name: rtsp-warden-db
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_USER: warden
  #     POSTGRES_PASSWORD: warden
  #     POSTGRES_DB: warden
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U warden"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5
  #   networks:
  #     - warden-net

# volumes:
#   postgres-data:

# networks:
#   warden-net:
#     driver: bridge